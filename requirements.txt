# Web
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9   # FastAPI Form(...) — /settings 엔드포인트

# Test
pytest>=8.0
httpx>=0.27        # FastAPI TestClient

# Desktop GUI
pywebview>=6.0

# (stdlib used: sqlite3, json, pathlib, datetime, dataclasses)
